name: Deploy to GitHub Pages (production / apex)

# Production deploy fires only on manual dispatch (verify-then-flip discipline).
# Staging deploy on push-to-main lives in deploy.yml — that workflow keeps building
# with BASE_PATH=/michelle_ngo_five for wolfwdavid.github.io/michelle_ngo_five/.
on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.0.9
          standalone: true

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build (apex — BASE_PATH unset)
        env:
          BASE_PATH: ''
        run: pnpm build

      - name: Assert prerender route count
        run: node scripts/assert-prerender-count.mjs

      - name: Verify CNAME in build artifact
        run: |
          test -f build/CNAME || (echo "build/CNAME missing — CNAME assertion not in artifact" && exit 1)
          echo "build/CNAME content: $(cat build/CNAME)"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: build/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
