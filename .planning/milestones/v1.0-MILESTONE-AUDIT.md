---
milestone: v1.0
audited: 2026-06-17
status: tech_debt
scores:
  requirements: 41/41   # satisfied for the live (GitHub Pages) deployment
  phases: 4/4           # all phases have VERIFICATION.md, all passed
  integration: 5/6      # 5 cross-phase flows fully wired; SEO chain partial in production
  flows: 5/6
gaps: {}                # no critical blocker for the currently-live staging deployment
tech_debt:
  - phase: 04-hardening-apex-cutover-launch
    items:
      - "DEFERRED (user decision 2026-06-16): apex DNS cutover to michellengo.net. Site is intentionally live on GitHub Pages; CUTOVER.md is the ready runbook. Milestone success criterion #4 (apex live over HTTPS) is intentionally not realized yet."
      - "PRE-CUTOVER BLOCKER (SEO-01/02/03): src/routes/+layout.svelte emits an UNCONDITIONAL `<meta name=\"robots\" content=\"noindex, nofollow\">` and static/robots.txt is `Disallow: /`. Correct for staging, but they are NOT gated by environment and CUTOVER.md does not remove them — an apex launch following the runbook as-written would ship the production site noindex, defeating all SEO/JSON-LD/sitemap work. Fix before the cutover: gate robots on BASE_PATH ('' = apex → index; staging → noindex) or strip in the production build, and add the step to CUTOVER.md."
      - "DEPLOY HYGIENE (FND-05): scripts/assert-prerender-count.mjs is wired into deploy.yml only, not deploy-production.yml. Documented decision (04-02 SUMMARY), but a production apex deploy can ship a drifted route count that staging would have caught. Low risk."
  - phase: 02-shared-components-card-destinations-seo
    items:
      - "SEO-01 minor: no page emits `og:title` or `og:url`. Social crawlers fall back to `<title>` and `rel=canonical` covers og:url's structural role, so the card still renders — but the explicit OG fields are absent. Low severity."
  - phase: 03-rails-homepage-cinematic-hero
    items:
      - "COSMETIC (D-13, no REQ-ID): ReelHero.svelte never renders `<div id=\"hero-sentinel\">`, so TopNav's scroll-aware transparency `$effect` (which looks it up by id) exits early and the nav stays solid even over the hero on `/`. Graceful fallback works; the designed transparent-over-hero treatment never activates. One-line fix in ReelHero."
nyquist:
  overall: not_assessed   # no *-VALIDATION.md files present in any phase; nyquist validation not run this milestone
  compliant_phases: []
  partial_phases: []
  missing_phases: ["01", "02", "03", "04"]
---

# v1.0 Milestone Audit

**Status: `tech_debt`** — All 41 requirements are satisfied for the live deployment and all four phases are verified `passed`. There are no blockers for the site as it currently runs on GitHub Pages. However, meaningful debt is parked against the **deferred apex cutover**, including one latent launch bug (production `noindex`) that must be fixed before michellengo.net goes live.

## Scope

Milestone v1.0, 4 phases, 12 plans, 41 requirements. Live at https://wolfwdavid.github.io/michelle_ngo_five/ (GitHub Pages, base path `/michelle_ngo_five`). The apex (michellengo.net) cutover is deferred by user decision.

## Phase Verification

| Phase | VERIFICATION.md | Status |
|-------|-----------------|--------|
| 01 Foundation, Data Reuse & Deploy | present | passed |
| 02 Components, Destinations & SEO | present | passed |
| 03 Rails Homepage & Cinematic Hero | present | passed |
| 04 Hardening, Apex Cutover & Launch | created during this audit | passed (9/9 must-haves; apex DNS flip recorded as deferred, not a gap) |

## Requirements Coverage (3-source cross-reference)

All 41 REQ-IDs cross-checked across REQUIREMENTS.md traceability, phase VERIFICATION.md tables, and SUMMARY frontmatter. **41/41 satisfied** for the live deployment.

- FND-01..06, DATA-01..04, DSGN-01..04 (Phase 1) — satisfied
- WATCH-01..04, BRWS-01..02, PBS-01, PAGE-01..03, SEO-01..03 (Phase 2) — satisfied (SEO-01 has minor og-tag debt + the production noindex caveat below)
- HOME-01..06, HERO-01..04 (Phase 3) — satisfied (HERO/DSGN: hero-sentinel cosmetic gap, no REQ-ID broken)
- QUAL-01..04 (Phase 4) — satisfied (axe 24/24, mobile LCP 2.05s/CLS 0/perf 98, responsive, reduced-motion; real-device QA human-approved)

No orphaned requirements. No `unsatisfied` requirement → FAIL gate not triggered.

## Cross-Phase Integration

**5 of 6 E2E flows fully wired** (verified against source):
1. Home rails → watch pages — all 56 base-path-safe links resolve to prerendered routes ✓
2. Rail "View all" → work/[category] — 8 slugs match `entries()` exactly ✓
3. Single reduced-motion gate (motion.svelte.ts) governs both ReelHero + CategoryRail ✓
4. Facade/zero-iframe invariant holds home→watch (build-time assertion enforced) ✓
5. Base-path correctness for staging ✓
6. SEO chain (canonical + JSON-LD + 70-URL sitemap) — structurally complete, **but defeated in production by the unconditional `noindex`** ✗ (production only)

## Tech Debt — Action Items (priority order)

1. **🔴 Before any apex cutover — remove production `noindex`.** `+layout.svelte` + `robots.txt` block all crawlers unconditionally. Gate on `BASE_PATH` (apex `''` → indexable; staging → noindex) and add the step to CUTOVER.md. Without this, the live site is invisible to search engines. *(SEO-01/02/03)*
2. **🟡 Apex DNS cutover (deferred):** follow CUTOVER.md when ready. *(milestone success criterion #4)*
3. **🟡 Add prerender-count guard to deploy-production.yml** for parity with staging. *(FND-05)*
4. **⚪ Add `og:title` + `og:url`** to each page's head (or layout). *(SEO-01)*
5. **⚪ Emit `<div id="hero-sentinel">`** at the bottom of the ReelHero section to activate the designed scroll-transparent nav. *(D-13, cosmetic)*

## Nyquist

No `*-VALIDATION.md` files exist for any phase — Nyquist validation was not run this milestone. Discovery only; not a blocker. Run `/gsd:validate-phase {N}` per phase if formal validation-coverage is desired.

## Verdict

The v1.0 site is **functionally complete and launch-ready on GitHub Pages** — every requirement satisfied, every phase verified, cross-phase wiring sound. The debt is concentrated at the **deferred apex boundary**, where item #1 (production `noindex`) is a genuine pre-launch fix, not optional. Recommend addressing item #1 (and ideally #3–#5) before flipping DNS; the milestone itself can be completed now with this debt tracked.
